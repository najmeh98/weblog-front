import { Post } from "../component/definition";

// export const sortByDate = (posts: Post, by: "created" | "updated") => {
//   const sortedTasks = posts.sort((a: Post, b: Post) => {
//     // if (by === "created") return +new Date(b.created) - +new Date(a.created);
//   });
// };

const nowTime = new Date().toLocaleString();
